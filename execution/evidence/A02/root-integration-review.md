# A02 integration review

The candidate updates Node to24.21.0 and applies seven narrow dependency overrides. Independent root comparison of the complete lock entries confirms all57 Aztec/Noir entries are unchanged. Undici, tmp, UUID and Jaeger cross parent version boundaries; their acceptance relies on actual consumer/API controls, not version strings alone. Jaeger retains its own core2.9 while the existing SDK/core1.x stack remains intact.

Fresh final exact-lock scans report48 affected package entries overall and19 in production, with no high/critical entries. The complete raw inventories remain preserved. This does not mean the software has no vulnerabilities. Eight older core paths and developer elliptic remain explicitly treated in residual-release-register.json; embedded TXE code is a separate scope limitation. Independent review accepts this bounded treatment, with D01-A05/T05-A05 controls still mandatory before release.

Native verification passed41 dependency,171 build,69 interface,155 moderation,62 Noir,22 Noir fixture,7 Solidity fixture,9 portal and two Docker isolation checks, five CLI lanes, both actual browser consumers and storage persistence. All supervised stages exited successfully with their process groups absent. The34 generated outputs were unchanged across checks. These are dependency/build compatibility checks; baseline regressions still deliberately demonstrate defects assigned to later contract/wallet packages. They do not establish real-proof settlement or production readiness.

The native run reused existing dependencies, compiler and pinned CRS caches. Independent clean Linux retry2 subsequently passed all19 stages and reproduced all34 native outputs exactly. Root separately checked the raw stage results, complete output maps, successful evidence extraction and owned-container absence. No native result is relabelled as a clean installation. The only change after native source attestation adds the missing dependency-test command to BUILDING.md; the staged Linux input comparison confirms no executable input changed.

Independent reviews cover dependency behavior/applicability, runtime/image pins, parser controls, native evidence and the Linux supervisor. The supervisor was repaired before launch so success requires evidence extraction, exact output comparison and verified container cleanup. External cryptographic/security review remains a distinct later gate.
