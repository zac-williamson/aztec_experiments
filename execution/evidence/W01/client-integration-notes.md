# W01 client integration inspection

The actual browser/CLI execution path is apps/src/billboard/user/engine.js (the censor engine is a symlink). Claim, real post, dummy post and withdrawal still send directly from the author. Its startup still recommends funding author FeeJuice; these must change before W01 completion.

The pinned SDK already exports NO_FROM. ContractFunctionInteraction.request concatenates options.authWitnesses into ExecutionPayload, and BaseWallet uses DefaultEntrypoint for NO_FROM. The existing custom wallet preserves ExecutionPayload through simulation/proving. A sponsor interaction must supply NO_FROM, additionalScopes:[owner], sendMessagesAs:owner, exact owner auth witness and capped gas settings. The explicit tag sender is necessary because the board delivers private notes and NO_FROM has no default tag sender. No reusable author appears as fee payer merely by adding this private PXE scope/tag option; actual transaction observations still require qualification.

Root inspected node_modules/@aztec/aztec.js/dest/contract/contract_function_interaction.js, node_modules/@aztec/wallet-sdk/dest/base-wallet/base_wallet.js and the actual engine implementation. This is source inspection, not a passing browser or sponsored-transaction test. Exact registered sponsor identity/board/version and coupon membership must be verified before requesting an author signature. Preserve send-once receipt reconciliation and distinguish a consumed fee ticket from retryable unsent work. Never retry a paid public revert with the same ticket or fall back to author funding.

## Genuine shared funding test

The current C01 preparation funds its author/deployer in genesis. W01 must separate the funded deployment/operator account from an unfunded author. The production sponsor constructor includes the board address, so do not force a guessed sponsor genesis address before the disposable rollup/board exists. Exercise shared replenishment through the actual L1 FeeJuicePortal bridge and an operator-paid FeeJuice.claim(to=sponsor,amount,secret,index) transaction. Pinned FeeJuice claim_helper authenticates the destination in the L1 message and credits that destination; it does not require recipient==caller. FeeJuice is not user-transferable, so an invented L2 transfer is not an option. Check zero author balance before/after claim/post/exit, sponsor increase on replenishment and actual fee debits. This also covers the real funding observation surface rather than relying only on a genesis fixture.

Source inspected: the embedded fee_juice_contract/src/main.nr in the installed official FeeJuice artifact and @aztec/aztec.js/dest/contract/protocol_contracts/fee-juice.js. This funding journey is still to be implemented and tested.

W01-F01 is recorded in the project risk register: the original fetch wrapper's substring credential test was reproduced against an in-memory observer and replaced with an exact parsed RPC origin/path boundary plus redirect rejection. The actual wrapper tests pass; integration with generated browser output is still pending.
