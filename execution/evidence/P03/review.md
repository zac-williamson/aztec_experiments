# P03 root integration review

Root reviewed all changed sources and the real outputs from each lane. Portal
checks execute generated bytes, compare both shipping copies against source,
and contrast the same invariants with a content-pinned historical artifact.
Permissive bridge calls establish accounting only. Root independently reran all
nine checks after rebuilding provenance; generated bytecode remained identical.

The receipt harness evaluates the entire shared production script, obtains its
actual wallet and runs sendTx with controlled SDK/node/prover boundaries. Actual
V5 receipt classes distinguish executionResult from inclusion status. It reproduces
both reverted and dropped false confirmations, while success/pending/error controls
check that the harness does not manufacture outcomes. Four duplicated engine
consumers are source-inspected only, explicitly assigned to W03 verification.

The historical shell fixture contains verbatim function bodies with mandatory
capture injection and no subprocess import. Another agent verified all provenance
hashes against upstream. Current signer gets exact inert argv, including canonical
wallet path. Tests never run the historical string through a shell.

Root added real private/utility history tests after the delegated lane's content
flag, then obtained separate AI review. The 16-post success case, 17-post full
state/page observation and 18th-call rejection together establish a local functional
boundary. The expected-failure annotation spans setup, so the companion successful
17-post case is necessary. Full suite reports 62 passing tests and exit 0.

Remaining assurance limits and repair destinations are in finding-matrix.md;
none are hidden by green known-bad tests. P03 changes test/config/documentation
only, with no production contract or receipt semantics repaired here. All current
M01 boundaries remain intact and tested. External review is still mandatory.

The new test-only Foundry output directory is ignored; production tracked artifacts
remain unchanged. Root excluded generated test output from the final source inventory.
