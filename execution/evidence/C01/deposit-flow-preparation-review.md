# Genuine local deposit/claim helper — prepared, not executed

Owned source: `scripts/c01-deposit-flow.mjs`, SHA-256 `f30c99cb978cea3f69e20bac463ea48e6d7b1ddc214bf33327f7053ec5391cca`. Pinned Node 24.21.0 syntax check passed. No wallet, transaction, test network, proof or heavy process was started by this lane.

Export: `depositAndClaimC01({node,preparation,instance,l1Client,ready,settlement,directory,rpcUrl,dateProvider})`. The parent supplies the same disposable account/instance and native launcher used for Board/Ready. It requires successful genuine settlement/activation observations plus live `depositsEnabled`, local chain 31337, matching portal/board/config/rollup, genuine node proof enforcement, native singleton options and current generated artifact/input hashes.

It submits a real portal deposit at the configured minimum, with a random nonzero in-memory Field secret. The actual single Deposited event must match depositor, incremented receipt nonce, amount and secret hash. Shared `encodeEscrowCommitment('claim',...)` plus pinned SDK SHA-to-Field and L1ToL2Message computes content/key. Deposit identity follows production `lib.nr`: Poseidon2 separator `0x42420101` over `[1,board,owner,content,secret]`. L1 receipt/block/active-escrow accounting is refreshed before and after later stages.

Inbox waiting uses ordinary `evm_mine` with the existing local date provider. The ordinary sequencer temporarily permits empty checkpoints; previous settings are restored in finally. This is the SDK's supported `buildCheckpointIfEmpty`/`minTxsPerBlock` path, not synthetic message insertion. PXE `autoSync:false` and explicit `sync()` select a checkpointed header. The actual membership path is folded with `DomainSeparator.MERKLE_HASH`, using a bigint index across all 36 levels, and compared to that header's message-tree root. The genuine `pxe.proveTx` result must contain exactly the same anchor. Normal `node.isValidTx` and ordinary `sendTx`/checkpoint SUCCESS are required.

After inclusion, the actual utility must return the exact initial eleven logical fields, including anchor timestamp plus minimum-deposit cooldown. The **test-only** `pxe.debug.getNotes` API reads the actual delivered packed note, filtered using the generated artifact's deposits slot, owner and contract. Its creating tx and all eight physical fields must match. `Owned<PrivateSet>.at(owner)` retains the generated slot; this is not a Map-derived slot. No production application debug-note API was added.

Only sanitized observations are enumerable. `result.claim` is nonenumerable and retains secret, private deposit identity, receipt/content, exact note data and tx in memory for a later no-post exit. Do not explicitly serialize it. Ephemeral wallet cleanup is mandatory; the parent owns node/prover shutdown, total deadline and descendant memory limits. Individual witness/inclusion waits are bounded to 120 seconds each; the native proof call relies on that parent supervision.

Source references checked: production `billboard/portal/src/BillboardPortal.sol` deposit/event/accounting; `billboard_contract/src/{main,lib}.nr` claim, packing and cooldown; installed `@aztec/stdlib/src/messaging/l1_to_l2_message.ts`, `hash/hash.ts`, `note/note_dao.ts`; `@aztec/pxe/src/pxe.ts` sync/anchor/prove and `debug/pxe_debug_utils.ts`; `@aztec/sequencer-client/src/client/sequencer-client.ts` and `sequencer/checkpoint_proposal_job.ts`; pinned native `world_state/fork.hpp` and `aztec/aztec_hash_policy.hpp` for message-tree hashing.

Remaining qualification: actual caller integration, live Inbox latency under the local timing/empty-checkpoint configuration, exact generated-note delivery/retrieval, proof resource capacity, and claim inclusion have not been run. The helper deliberately does not mark the claim's covering epoch proven or perform an exit/refund; those require the parent's subsequent genuine settlement flow. It is not a production wallet custody or fee-privacy implementation.
