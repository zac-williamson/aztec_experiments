# Final local L1 withdrawal helper — prepared, not executed

Source: `scripts/c01-withdraw-l1.mjs`, SHA-256 `1eab2a8f6a160d8ba3572df282cff766a01453d52125ea49f0ec8ac3820cc7c8`. Pinned Node 24.21.0 syntax check passed. No transactions, proofs, wallets, downloads or heavy processes were run in this lane.

Export `withdrawC01L1({node,preparation,ready,exitResult,settlement,l1Client,rpcUrl})` requires a passed exit helper with its in-memory context and a passed genuine settlement observation containing `finalized`, `targetCheckpoint`, and `proofReceipts`. It does not schedule epochs or treat those booleans as sufficient evidence.

Before withdrawing it refreshes the successful canonical exit receipt, block checkpoint, actual finalized node tip, actual L1 `finalized` block, and historical `RollupContract.getProvenCheckpointNumber({blockNumber})`. At least one supplied covering proof transaction must remain canonical, successful and finalized, and its **actual receipt logs** must contain the corresponding Rollup `L2ProofVerified` event covering the exit checkpoint. The actual canonical TxEffect must still contain the expected exit leaf. These checks run again after fetching the genuine Outbox witness, before the L1 send. The finality scope is Anvil's actual finalized tag, not Ethereum economic-finality qualification.

The same disposable L1 depositor signs `portal.withdraw`. Local chain 31337, portal enabled state, rollup/board/version/Outbox/config bindings, active nonce/amount and current artifact/input hashes are checked. A canonical successful receipt and exact Withdrawn event are required. At the withdrawal block, escrow must clear; `totalDeposited` and portal balance must each fall by exactly the amount. Depositor balance must increase by amount minus `gasUsed * effectiveGasPrice`, with no blob gas. A real `simulateContract` repeat must revert specifically with `No active deposit`; no second transaction is sent.

Accounting compares the receipt block to its preceding block. It fails if another transaction from the depositor appears in that block, rather than silently attributing unrelated gas to the withdrawal. The parent must serialize that fresh account's uses; unexpected concurrent transfers will also fail the exact balance equations. No secret or private deposit identity is included in enumerable output or errors. A missing witness, stale canonical observation, wrong revert reason, or balance mismatch fails qualification.

Checked source: existing `c01-settle-ready.mjs` proof/finality observations, current portal withdraw/event/accounting, installed RollupContract/ABI, node `getChainTips`/`getBlock`/`getTxEffect`/`getL2ToL1MembershipWitness`, and viem `ContractFunctionRevertedError.reason`. Actual supported shared exit encoding and SDK envelope hash are reused. Existing helpers remain unchanged.

Not yet qualified: caller integration with generalized exit settlement, live witness availability, canonical withdrawal and all accounting/repeat controls. Parent supervision must bound the overall operation and owns node/prover lifecycle. No C01 acceptance or production-readiness claim follows from preparation alone.
