Each listed scalar occupies its own canonical Field in the note's Packable representation; the application must enforce canonical integer, boolean and address encodings at the relevant boundary. Declare both note types public and derive the matched compiler's note traits. Actual Noir beta25/Aztec 5.2 derived packing is **11 Fields for DepositNote and 7 Fields for PostNote**, in exactly the orders above. Every generated typed selector has its zero-based field ordinal as `index`, byte `offset=0`, and byte `length=32`; these are whole canonical Field selections, not scalar bit widths. In particular, both chain-ID selectors use index 1 and the PostNote sequence selector uses index 2. Use generated typed selectors, not manually reconstructed offsets. Do not infer address range safety from raw unpacking: the matched `EthAddress::unpack` retains its inner Field, so explicitly call its supported `validate()` or apply the same constrained 160-bit bound before using any arbitrary-origin unpacked L1 address. Actual matched `u32`/`u64`/`u128` raw `Packable::unpack` casts truncate over-width Fields; unpack alone is not rejection. At an application interface that receives and retains raw packed Fields, require `unpack(raw).pack() == raw` before accepting the typed result, and separately validate EthAddress and application schema/economic bounds. Bool unpack already rejects values other than 0/1. For an already typed HintedNote, do not invent unavailable pre-deserialization raw witness access: actual membership must authenticate the note hash computed from the canonical typed note serialization, with the owner/slot/contract/ancestry checks below. A typed value produced by truncation does not by itself prove membership of the original malformed packed note. C02 must test this actual end-to-end boundary, not rely on the pure unpack fixture as proof assurance. The executable fixture `scripts/fixtures/noir-interface-v1/src/lib.nr` passes exact arrays, round trips, all generated selectors and full scalar-width controls. This establishes packing and selector construction only; actual owned-note retrieval and proof authentication remain C02/C04 tests.

