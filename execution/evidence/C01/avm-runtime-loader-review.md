# Native AVM runtime loader — implementation, not proof qualification

Owned source `scripts/c01-avm-runtime.mjs`, SHA256 `3f9d69c7aa5e517406b09348932458cb6e5da55171fadba643db47450223ba1a`.

API: `resolveC01AvmRuntime(absoluteManifestPath)` returns `{binaryPath, provenance}`. It performs reads only and never executes a native binary, starts a build, or produces a proof. The manifest must be precisely `.build/C01-avm-native/<UUID-v4>/manifest.json` beneath the canonical repository root. Every owned path component is checked without accepting symlinks. Files are opened with O_NOFOLLOW, bounded, streamed through SHA256, and checked for inode/size/time changes or replacement during reading.

Acceptance requires schema 1; matching build UUID; `passed`, `sourceUnchanged`, `copiedSourcesUnchanged`, and `temporarySourceCopyVerified`; both `cleanup.descendantTreesAbsent` and `temporaryTreeRemoved`; no recorded failure/interruption/deadline; the declared two-job, 25-minute, 8-GiB descendant-supervised profile; and all expected stages exiting successfully with observed cleanup and sampling. Group-only historical manifests cannot pass these checks.

The loader binds the reviewed builder SHA256 `56f307a61d47cf76619ac75fdee68fcc93a7aa99c70aadbce6b4b30168c91275`, origin report SHA256 `c7eb8ce8a95002e66ab8eed464f409c66b62c1d16535df712c2f11ce77bdfd75`, official archive SHA256 `8963060f6f30ffff69825c4aeaa861d6b4e3b2c60faa522f5fc59e8075fe1c0b`, and commit `49a592109ec4f18d79212b43d621891aaf36f7b6`. The build's complete original C++ file/link inventory must equal the authenticated report's inventory. A future builder/provenance change requires deliberate loader review rather than silently accepting new semantics.

The binary must be the local `bb-avm`, executable, between 1 MB and 512 MiB, and match the manifest's exact byte count and whole-file digest. CMakeCache is bounded to 4 MiB, checked against its recorded whole-file digest, and its actual read byte count is returned. The current builder records no separate cache byte-count pin, so the loader does not invent one. Parsed settings require AVM ON, BB_LITE OFF, Release, PIC, the explicit Xcode SDK, Ninja, and no embedded artifact-transpiler library. Duplicate cache keys are rejected. Compiler wrapper paths and exact wrapper content digests must agree with the reviewed LLVM20 `--no-default-config`/SDK wrapper, and recorded compiler identities must identify LLVM20.

Sanitized provenance contains only public local build identities, byte counts/digests, resource observations and compile settings. It explicitly says an unmodified source-build version string is not relabeled 5.2.0. Source commit and verified bytes identify this candidate; real AVM API and proof qualification remain separate requirements. A locally editable manifest is engineering evidence, not a signed remote attestation or protection against a privileged host replacing both evidence and software.

Validation performed: pinned Node 24.21 syntax check exited 0; diff whitespace check exited 0. No successful manifest was synthesized, no loader acceptance was claimed for the still-running build, and no built binary/prover/network action was executed. Root must run the loader against the genuine final manifest and qualify actual runtime behavior before integration into an accepted covering epoch proof.
