# Generic genuine message settlement preparation

Source: `scripts/c01-settle-message.mjs`, SHA-256 `494071a1a9e319e551dc5ce106c66cf2e43e7c8c6f8581cbb339865951c89982`. Pinned Node 24.21.0 syntax check passed. No proof, network, build, or transaction was executed in this lane. The earlier running settlement inputs were not edited. Root reported the earlier Ready run passing before authorizing the new exit-specific budget.

Export `settleC01Message({node,config,dateProvider,l1Client,directory,rollupAddress,txHash,expectedLeaf,kind,proofSearchFromBlock,startProver})`. `kind` is `ready` or `exit`. Root supplies the real portal-deployment L1 block as `proofSearchFromBlock`, and starts the prover exactly once (`startProver:true` for first Ready, false for the later exit). The installed `ProverNode.start()` creates services; it is not treated as idempotent. The helper does not stop the shared prover. Root must replace the existing Ready scheduler with a wrapper for the next full-bridge run; this new module temporarily coexists for that integration, not as a second permanent scheduler.

The scheduler is extracted from the current genuine Ready helper: canonical checkpoint headers determine epochs, at most 128 unproven checkpoints are scheduled serially, existing active sessions are reused, actual `startProof` drives remaining work, and L1 proven-checkpoint advancement determines completion. Failed/cancelled checkpoint subtrees and failed terminal jobs reject. The existing sanitized circuit/job observer is retained. The helper keeps the declared 20-minute Ready watchdog and uses the separately declared 30-minute exit watchdog for its additional Inbox/claim/exit checkpoints, ordinary L1 mining and date synchronization, local 31337 real-proof configuration, 64-epoch proof window and one pending job/agent. The parent separately owns native thread configuration, the 8 GiB descendant cap, total timeout and complete process/node/prover cleanup. The helper watchdog does not itself cancel an in-flight SDK call.

Initial and refreshed checks require a successful canonical L2 receipt, matching public BlockResponse hash/checkpoint, and actual canonical TxEffect containing the expected message leaf exactly once. A target already proven at entry is allowed only when the specified real L1 search range contains a covering `L2ProofVerified` event. Each retained event must occur in a successful canonical receipt with identical raw log/address/index/topics/data. Final acceptance requires the covering receipt to be under the actual L1 finalized tag, the Rollup's historical proven-checkpoint value at that tag to cover the target, and the node's finalized L2 tip to cover the message block. Message canonicality and covering finality are rechecked after obtaining the actual node Outbox membership witness. No node flag alone qualifies settlement.

Return fields include `passed`, `kind`, `targetCheckpoint`, `targetAlreadyProvenAtStart`, `proofReceipts`, `finalized`, `membership`, epochs/jobs and sanitized progress. The actual witness is the nonenumerable `.witness` property for a later activation/withdrawal caller. It is not serialized into evidence. Progress files are `settlement-ready-progress.json` and `settlement-exit-progress.json` in the owned directory. There is no portal read/write, activation, withdrawal, synthetic root or proof insertion in this helper.

Checked installed source: `@aztec/prover-node/src/prover-node.ts` (`getJobs`, `startProof`, `start`), `checkpoint-store.ts` and `job/checkpoint-prover.ts` (`isFailed`, `isCancelled`); `@aztec/aztec-node/src/aztec-node/server.ts` membership delegation; `@aztec/ethereum/src/contracts/rollup.ts` epoch and historical proven-checkpoint reads; installed Rollup ABI proof event; current Ready settlement and final L1 withdrawal helpers for receipt/finality shape. Limits: no integration execution yet, no independent mathematical revalidation of the returned Outbox path in this helper (the subsequent genuine portal call must validate it), and Anvil's actual finalized tag is not Ethereum economic-finality qualification. The caller is responsible for supplying the genuine portal-deployment search origin and preserving its deployment evidence.
