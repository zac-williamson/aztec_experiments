# No-post L2 exit helper — prepared, not executed

`scripts/c01-exit-flow.mjs` SHA-256: `e8c6d8bdf55cf04961489e58f2583e437ae5909534f0a40e836d249bad2cb58b`. Pinned Node 24.21.0 syntax check passed. No proof, process-heavy test, account creation or transaction was run by this lane.

Export `proveAndIncludeC01Exit({node,preparation,instance,claimResult,l1Client,directory,rpcUrl,dateProvider})` requires the preceding passing deposit helper and its nonenumerable in-memory `claim` context. It reopens the same ephemeral proving wallet, verifies current artifact/input hashes and local real-proof node scope, and reads the exact claimed note using both the eleven-field utility and TEST-ONLY `pxe.debug.getNotes` with explicit `NoteStatus.ACTIVE`, generated storage slot, owner and board filters. All eight packed values and its creating transaction must match the no-post receipt; its actual siloed nullifier is retained in memory.

Eligibility uses ordinary disposable L1 mining and PXE synchronization to a checkpointed anchor whose timestamp reaches the actual note's `next_allowed_time`. Normal empty-checkpoint settings are temporarily enabled and restored in finally. No clock warp, state override, fabricated root or synthetic note is used. `autoSync:false` prevents a hidden anchor switch; actual proof output must equal the selected header. The helper follows genuine `withdraw(chain)` request/fee/`pxe.proveTx`, normal node admission, send, and successful ordinary checkpoint inclusion.

Exit content uses the maintained shared escrow encoder and pinned SHA-to-Field; envelope hashing uses actual SDK `computeL2ToL1MessageHash`. The canonical included TxEffect must contain exactly one expected exit leaf and exactly one original note siloed nullifier. After synchronization, the logical utility must return eleven zeros and the active-note diagnostic must contain neither that nullifier nor a replacement note with the private identity. The emitted TxEffect and refreshed receipt are tied to their canonical block.

Only sanitized observations are enumerable. Nonenumerable `result.exit` retains the tx, claim context, receipt, instance and message fields for root's later settlement step; never explicitly serialize it. Wallet stop and sequencer configuration restoration run in finally. Parent supervision owns proof duration/resources; eligibility and inclusion waits each have a 120-second bound.

Checked implementation references: production `main.nr` withdrawal at lines 545–566; installed `@aztec/stdlib/src/hash/hash.ts` message envelope hashing; `tx/indexed_tx_effect.ts` and `tx/tx_effect.ts`; `note/note_status.ts`/`note_dao.ts`; `@aztec/pxe/src/debug/pxe_debug_utils.ts`; the previously prepared deposit helper and actual Ready inclusion path. Existing files remain unchanged.

Unqualified until executed: native proof capacity, exact account note rediscovery, eligibility progress under actual local sequencing, inclusion and nullifier observation. This helper does not accept an epoch proof, request an Outbox witness, withdraw L1 ETH, or claim production wallet readiness. Root must perform genuine covering settlement and L1 accounting separately.
