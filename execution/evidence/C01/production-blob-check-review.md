# Production BlobLib selection checker — authored, not executed

2026-09-14. `scripts/c01-check-production-blob.mjs` SHA256 `39ea262285f31754720e13dc87a616200a76d7c33405a22b409c9c34fc05f690`. This lane ran no compiler, test, deployment or RPC. Root owns runtime qualification, forced-build evidence and cleanup.

API: `checkC01ProductionBlob({client, rollupAddress, buildDirectory, allowedRoot})`. Call before the SDK's exit cleanup removes its prepared checkout. `buildDirectory` is the wrapper-recorded `.foundry-deploy-*` cwd; `allowedRoot` is the parent-owned private root. Client is the existing local read client. Returns sanitized observations only, including code/source/artifact hashes and the exact qualifications/exclusions below.

## Actual compiled/deployed path

BlobLib's functions are internal and inlined; there is no independent BlobLib deployment address. Installed RollupCore.submitEpochRootProof delegates to RollupOperationsExtLib.submitEpochRootProof, which invokes the internal EpochProofLib path; EpochProofLib imports the configured BlobLib. Therefore the check follows **Rollup runtime link → deployed RollupOperationsExtLib runtime → compiled production BlobLib source**.

The check reads fresh `out/Rollup.sol/Rollup.json` and `out/RollupOperationsExtLib.sol/RollupOperationsExtLib.json`, requires exact compilation targets, solc0.8.30, the production remapping, production BlobLib in each compiled source closure, and no src/mock source in those closures. Every metadata source keccak must match both the actual staged source and its installed source bytes. Symlinked/out-of-root artifact/source paths fail. The wrapper's successful forced build is independently supplied by root; the checker does not pretend a metadata profile alone proves a build occurred.

It reads every Rollup deployed-library address from the artifact's declared20-byte link references, rejects zero or inconsistent repeated addresses, and matches all other runtime bytes except the three explicitly declared32-byte constructor immutable slots. Substitution ranges must fit and not overlap. It obtains RollupOperationsExtLib from that actual link, then requires its entire deployed runtime to match the fresh production artifact, including compiler metadata. The sole library-specific substitution is Solidity's PUSH20 self-address constructor patch; the expected zero-address/PUSH20/ADDRESS/EQ prefix is checked before inserting the actual deployed library address. Nested link or immutable references in that library are rejected. Artifacts are reread/hash-compared at completion.

## Exact limits

Returned `productionBlobLibQualified:true` means the above production code-selection/runtime checks passed. It is not successful blob/KZG validation, epoch proving, or L1 proof acceptance. Other linked Rollup libraries are recorded but not independently code-qualified by this narrow checker. The three Rollup immutable values/offsets are explicitly returned with `immutableSemanticsQualified:false`; they are not described as checked constructor semantics. Parent separately verifies the actual Honk verifier, and must retain all genuine bridge acceptance checks. Root's trusted freshly forced build and source attestation are part of the evidence boundary; this is not a defense against an adversarial local compiler or same-UID modification.

Source references: installed `src/core/RollupCore.sol:468–469`, `src/core/libraries/rollup/RollupOperationsExtLib.sol:10–11,42–43,96`, `src/core/libraries/rollup/EpochProofLib.sol:5,436`, and internal functions in `src/core/libraries/rollup/BlobLib.sol`. Existing default-profile artifacts show both core and mock BlobLib in the closure; their metadata must fail this check. No dependency, production contract, deployment module or wrapper was changed in this lane.
