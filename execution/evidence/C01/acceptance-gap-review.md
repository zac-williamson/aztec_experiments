# C01 acceptance coverage and smallest remaining controls

Read-only preparation during the full bridge run. No source edits, tests, proof jobs or chain calls were made. A running experiment is not passing evidence. Historical `noir-focused-3.log` reports 37 tests passed and `noir-full-1.log` reports 101 passed; earlier `noir-lifecycle-5.log` is a preserved failed discovery attempt, not the final outcome. `portal-focused-final.log` reports 26 passed and excludes generated-artifact tests; `portal-full-1.json` separately contains the later full source/generated-artifact results. Root must bind final acceptance to current source/artifact inventories, not just these counts.

| Criterion | Existing maintained controls | Genuine helper coverage / remaining boundary |
| --- | --- | --- |
| C01-A01: intended collateral creates permitted rights | `c01_claim_delivers_exact_v1_receipt_and_initial_eligibility`; compact note exact selection and independent burn; portal minimum/maximum and single-active-receipt tests | Deposit helper checks actual L1 receipt/message identity, genuine claim, exact eleven logical values and exactly one delivered eight-field note. Running result is still pending; later posting semantics remain C02–C05. |
| C01-A02: wrong scope/amount/secret/unbound reject | `c01.nr`: wrong portal origin, recipient envelope, nonzero secret, zero secret, receipt nonce, above-max amount, unbound portal, wrong initialized chain/version. Portal tests mutate every Ready/exit content word, actor/version/chain envelopes and membership arguments; constructor/bridge identity bounds remain checked. | Current genuine run is predominantly a positive path. Two small missing *direct L2 content-authentication* controls are an altered nonzero depositor and an altered amount that remains within min/max: existing above-max test rejects the economic bound before proving message mismatch. These can reuse the existing valid TXE message and change exactly one claim argument, expecting message absence. No extra production ABI is needed. |
| C01-A03: no repeated claim/withdrawal; matching identities | TXE `c01_claim_replay_cannot_mint_right_for_second_owner`; `c01_compact_second_burn_rejected`; portal repeated activation, repeated withdrawal, and redeposit with fresh nonce rejecting old exit proof. Shared frozen claim/exit vectors and actual portal Inbox key tests bind domain encoding. | Actual deposit helper compares event/SDK message identity and resulting private identity. L1 refund helper already performs actual repeated-withdraw simulation expecting `No active deposit`. **Actual-chain repeated claim is absent from the running bridge helper.** Add the bounded control below after current source freeze ends. |
| C01-A04: integrated flows without consumption/proof bypass | Actual production `consume_l1_to_l2_message` is used; TXE and canonical Inbox/Outbox contract tests preserve their explicit controlled-environment limitations. Earlier Ready run `real-network-962b9b9d-8502-44a7-8fcc-8357a4860267.json` passed genuine client/server proof publication, finalized observation and activation. | Current full run must still pass actual deposit/claim/exit/refund and cleanup. Portal test roots are supplied by a controlled rollup fixture and are not genuine rollup proofs. TXE execution, including its documented coherent chain/version service adapter, is not native proof acceptance. |
| C01-A05: real eight-field storage and exact logical view | `compact_note.nr`: macro note limit, exact maximum packed values, generated chain/time selectors, asymmetric sequences, invalid bit96/bit192 and truncation-above128 rejection, actual TXE delivery/select/burn, two-receipt selection and independent burn, discovery offset arguments. | Running claim checks actual delivered eight fields and eleven-field utility. Running exit checks emitted exact note nullifier plus absence of active original/replacement rights. These remain unobserved until the genuine run completes. No need to repeat obsolete eleven-field storage fixture as current acceptance. |

## Small actual-chain repeated-claim control

After first claim inclusion and PXE synchronization, while the same wallet/account and in-memory receipt secret remain available and before exit, derive the exact message nullifier: `inner = poseidon2HashWithSeparator([claim.message.hash(), claim.secret], DomainSeparator.MESSAGE_NULLIFIER)`, then `siloNullifier(boardAddress, inner)`. This follows pinned `aztec/src/hash.nr:51–52`, `messaging.nr` and `PrivateContext.consume_l1_to_l2_message`; the secret array here has exactly one Field. Do not assume a fee-juice-specific helper is generally correct for arbitrary contracts. Record only public/nullifier hashes, never the secret or private identity.

Require this siloed value exactly once in the first claim's canonical `node.getTxEffect(...).data.nullifiers`, plus `node.getNullifierMembershipWitness(selectedAnchor, siloed)` present. Confirm the original Inbox leaf still has membership at the same anchor. Then make a **fresh** `board.methods.claim_deposit(sameDepositor,sameAmount,sameNonce,sameSecret,sameLeafIndex).request()`, followed by the existing completeFeeOptions/createTxExecutionRequestFromPayloadAndFee/PXE.proveTx path. Installed BaseWallet creates `txNonce: Fr.random()` for a new account request, avoiding a mere duplicate transaction replay. Expect the actual message-consumption witness lookup to reject with `No non-nullified L1 to L2 message found`; catch/check that specific reason in memory and retain only a sanitized category. Pinned `stdlib/src/messaging/l1_to_l2_message.ts:getL1ToL2MessageWitness` checks both message membership and siloed nullifier presence at the reference block. Keep the first exact note/receipt unchanged and verify no second note was created. If proof unexpectedly succeeds, fail the control rather than sending it or accepting an unrelated nonce/fee error.

That is an actual PXE/protocol replay-prevention control against a real consumed message; it is **not** a mined failed transaction or a second accepted proof. The prior first claim is its genuine proof/receipt normal control. A cross-owner replay is already covered by the maintained TXE test; a second funded real account is unnecessary for this smallest increment. A fresh L1 redeposit/old-exit misuse case is also already covered with canonical Outbox Solidity tests and need not be folded into the first capacity-constrained bridge experiment.

No acceptance criterion is waived here. The minimum remaining evidence is a passing current full bridge run, the actual-chain repeated-claim control, the two discriminating direct claim-content negatives, and final source-bound integration checks. Separate external audit, production fee privacy, broader wallet recovery and later contract semantics retain their graph gates.
