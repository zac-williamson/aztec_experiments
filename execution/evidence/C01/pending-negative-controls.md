# Pending C01 negative controls — not applied

Prepared `pending-negative-controls.patch`, SHA-256 `a33ec0e49c8eee9e479d9cef6d5598f13b1ffb292ce2f5f03704edf13e175ac2`. No running source was changed, no test/proof/network execution was performed, and no passing result is claimed. Root must review/apply after the current frozen run, refresh affected artifact input manifests for the Noir test change, and execute the affected controls.

| Path | Required base SHA-256 | Candidate SHA-256 |
| --- | --- | --- |
| `scripts/c01-deposit-flow.mjs` | `38ecb19c6bce842b5c33352acbe6b949b499197b75ddc7218b8b589ddf53664a` | `7f89e37a37ba52c563912e1e4c87550ed3785e033a0fafcdd593747f34702d10` |
| `billboard/billboard_test/src/c01.nr` | `7885116781244ea6adc61b0c8abcab0a1144f58bb9fda880a3f445680be613bb` | `7da6795e594a42907af38e415c9763f7805b521aa7b7936945a137d67db6e608` |

The genuine helper addition runs after successful first claim and exact note validation. It checks the correctly siloed message nullifier exactly once in the canonical claim TxEffect and checks its actual membership key at the selected post-claim PXE anchor. The original Inbox leaf must still exist there. A fresh account execution request repeats the same claim arguments; request bytes must differ from the original (pinned BaseWallet generates a fresh `Fr.random()` txNonce). Actual `pxe.proveTx` must fail specifically with the consumed-message witness error **for this exact message hash**, inspected only in memory through at most eight causes. A fee, nonce, missing-message, setup, or unrelated simulator error cannot satisfy the control. Unexpected proof success fails without sending a second transaction.

The same anchor must remain selected/canonical during rejection. After synchronization, all eleven logical values, the sole active eight-field note, its creating tx/nullifier and the original L1 receipt/accounting must remain unchanged. The new enumerable observation stores only booleans and the fixed category `consumed-claim-message`; it contains no secret, private identity, request bytes, error message or witness. This is an actual PXE/protocol replay rejection against a genuinely consumed message, not a mined failed tx or a second accepted proof.

Pinned source checks: `aztec/src/hash.nr` hashes `[message_hash].concat(secret)` with the message-nullifier separator; this application supplies exactly `[secret]`. SDK `siloNullifier`, actual `getNullifierMembershipWitness(...).leafPreimage.getKey()`, `TxExecutionRequest.toBuffer()` and exported `NoteStatus.ACTIVE` match the addition. `stdlib/src/messaging/l1_to_l2_message.ts:getL1ToL2MessageWitness` provides the exact rejection text; SimulationError preserves its original message while adding contextual text. No oracle behavior or proof constraints are replaced.

The two TXE additions reuse a correctly bound board and valid intended message, then change only a nonzero depositor or the amount from MINIMUM to MINIMUM+1 (explicitly still below MAXIMUM). Both must reject with `No L1 to L2 message found`, demonstrating content authentication rather than economic-bound validation. Existing valid claim and above-max controls remain. These are TXE checks with their existing explicit proof limitations, not new genuine proof acceptance.
