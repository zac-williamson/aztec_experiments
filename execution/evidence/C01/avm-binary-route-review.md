# AVM binary route: read-only source findings

2026-09-14. No binary execution, download, configure, build, container, or proof run in this lane.

The observed epoch failure matches intentional upstream binary separation. In the cached pinned source `/Users/zac/nargo/github.com/AztecProtocol/aztec-packages/v5.2.0/barretenberg/cpp`, `src/barretenberg/bb/CMakeLists.txt` links `bb` to `vm2_stub` and `bb-avm` to genuine `vm2`. Both use the same main, CLI, general API and native IPC modules. `vm2_stub/api_avm.cpp` emits the observed unsupported-AVM error. `vm2_stub/avm2_recursion_constraint.cpp` also rejects AVM recursion, so assigning only PUBLIC_VM jobs to a full binary may not suffice for later recursive server jobs.

Pinned `@aztec/bb-prover/src/config.ts` has one `bbBinaryPath`; there is no separate AVM binary setting. `prover/server/bb_prover.ts:118` constructs one `BBJsFactory` from it. `bb/bb_js_backend.ts` uses NativeUnixSocket and calls actual `api.avmProve`/`api.avmVerify`. The native backend launches `msgpack run --input <socket>`; a wrapper dispatching on the CLI command `avm_prove` cannot route these API requests. Point the server prover's configured binary path to a full `bb-avm` launcher, preserving the one-thread limit. Upstream AVM proving tests and prover-client fixture configuration already select `bb-avm`.

## Practical local next experiment

Native source build is plausible, not qualified. Existing paths include CMake, Ninja, `/opt/homebrew/opt/llvm@20/bin/clang++`, and matching pinned C++/generated VM sources. No `cpp/build/bin/bb-avm`, existing `_deps` tree or transpiler static library was present. `CMakePresets.json:homebrew` selects Homebrew LLVM20 and inherits default Release/Ninja configuration. `AVM` defaults ON; the inspected real `vm2` and `bb-avm` targets have no Darwin exclusion. `OMP_MULTITHREADING` defaults OFF, so missing standalone Homebrew libomp is not a default-build blocker. Zig was not found on PATH; the upstream clang20/cross presets require it, whereas homebrew does not.

Use a fresh project-owned out-of-source build directory and the equivalent homebrew/default compiler settings, explicit `-DAVM=ON`, then build only target `bb-avm` with `-j1`. Do not build into or mutate the shared cached source. Keep existing memory/deadline supervision; successful compilation and AVM proof capacity remain to be measured. Configuration itself performs downloads: Tracy `5d542dc09f3d9378d005092a4ad446bd405f819a`, nlohmann JSON `9cca280a4d0ccf0c08f47a99aa71d1b0e52f8d03`, googletest v1.13.0, Aztec google-benchmark v0.0.1, cpp-httplib v0.15.3, libdeflate v1.24. Build-time external projects include Aztec msgpack-c `c0334576ed657fb3b3c49e8e61402989fb84146d` and LMDB `ddd0a773e2f44d38e4e31ec9ed81af81f4e4ccbb`. Bind tag resolutions/content and tool identities before treating a local build as reproducible.

The homebrew/default preset does not set optional `AVM_TRANSPILER_LIB`. Keeping it unset leaves artifact-transpilation convenience disabled; CMake still builds real `vm2` and `bb-avm`, and the AVM proof/verify APIs are not conditional on that flag. The flag's inspected uses are CLI help and `api/aztec_process.cpp:transpile_artifact`. The application already has processed artifacts. This is separate from disabling AVM constraints. Reproducing the release preset with embedded transpiler would require its static library, Rust 1.89 and the missing `noir/noir-repo` source submodule; that is a larger dependency task.

## Official Linux alternative

Root-fetched official metadata `/private/tmp/c01-v5.2.0-release.json` lists only `barretenberg-avm-amd64-linux.tar.gz` for AVM, 17,847,014 bytes, SHA256 `e573e15e0d808a751df001893e1fa87d4cbde0dafd3bd74e4f6c9ee8af1eb05d`, at `https://github.com/AztecProtocol/aztec-packages/releases/download/v5.2.0/barretenberg-avm-amd64-linux.tar.gz`. No Darwin AVM release asset is listed. Cached `cpp/bootstrap.sh:build_release_dir` agrees: it packages native Linux bb-avm while cross-platform release archives contain lightweight bb.

A publisher-built alternative is an isolated linux/amd64 Node + full bb-avm server-prover environment, with pinned dependencies and verified setup, under Docker's emulation on this ARM host. Parent reports the existing Docker VM has about 8.2 GB and no prepared amd64 runtime, so this is not an already-ready fallback. Keep the Node prover and its native Unix socket in the same Linux environment. A host-to-Docker executable shim is not a transparent substitute for NativeUnixSocket. Supported broker/agent separation exists through `proverBrokerUrl`/`PROVER_BROKER_HOST` and `proverAgentProofTypes`/`PROVER_AGENT_PROOF_TYPES`, but requires deliberate topology, cleanup, confinement and compatibility qualification; no such integration was run here.
