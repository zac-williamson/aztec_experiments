# C01 Ready preparation module — authored, not executed

2026-09-14. `scripts/c01-ready-flow.mjs` SHA256 `a194a70fe40bd01adbb4356b12bee4e96f999b4c2db594a5f7e1e9ac268094e9`. No test, proof, deployment, or RPC ran in this lane. Root owns serial bounded execution and all later integration.

Export: `prepareAndProveC01Ready({node,preparation,instance,deploymentReceipt,l1Client,directory,rollupAddress,rollupVersion})`. Inputs reuse the first board-flow preparation/instance and actual local protocol deployment signing client. Requires a checkpointed-or-later SUCCESS board deployment receipt and refetches its actual receipt before wallet work. The exact12-argument board deployment has therefore executed before reading its public immutable configuration. This is a checkpointed precondition, not finalized proof acceptance.

The helper recreates the same initializerless account in a fresh proving-enabled wallet, registers the existing board instance and source-bound artifact, and synchronizes PXE. It reads `get_config_hash`, `get_min_deposit`, `get_max_deposit` through actual wallet public-view simulation; these getters are public views, not Noir utility methods. It requires `is_portal_set=false`. Generated board/portal artifacts must match the current contract manifest and complete input inventory; portal creation bytecode must have no unresolved link references.

Using the supplied fresh Anvil signing client, it deploys the real portal with exactly `(rollup, board, version, minDeposit, maxDeposit, configHash)`, waits for a successful L1 receipt, checks nonempty code and scope/economic immutable getters, and requires deposits disabled. No test collateral is deposited. Then the actual existing private `update_portal` method is requested/proved with the same account and real native single-thread singleton. There is no `bind_portal` function in the current ABI. Default `node.isValidTx` must accept the resulting nonempty proof; no simulation or fee-validation bypass is supplied. Binding transaction is not sent, and unbound/disabled state is checked again.

Wallet.stop runs in finally; parent retains global BB/node/L1 lifecycle ownership. The returned top-level observation is JSON-safe; exact tx and instance are nonenumerable properties for later in-memory continuation. On failure, only fixed stage/error class and already-public deployment observations survive. Private account material, full requests, proofs and tx objects are not logged. The passed-in signing client remains a secret-bearing runtime object and must not be serialized.

## Next actual Ready evidence

Expected content is computed using maintained `encodeReadyCommitment` and actual pinned `sha256ToField`. Canonical Outbox leaf is SHA256-to-Field of board32 || version32 || portal20 || L1chain32 || content32. The helper records these only as **expected** values. `update_portal` enqueues public `_set_portal_public`; the Ready message is emitted only when that public call actually executes in a successful included binding transaction.

Root must next submit the exact returned tx, require CHECKPOINTED/SUCCESS and the actual emitted leaf/message identity, obtain genuine covering epoch proof acceptance on L1, and wait for finalized observation before requesting `getL2ToL1MembershipWitness(txHash, leaf, messageIndexInTx?)`. That witness supplies epochNumber, numCheckpointsInEpoch, leafIndex and siblingPath for the actual portal.activate call. A missing witness is not replaced with a constructed root/path. Disabled portal state, expected content, client proof and node admission are all insufficient to claim Ready activation or C01 completion.

Source references: current main.nr public getters650–690 and private update_portal/public only-self setter196–216; portal constructor36–56 and activate; SDK wallet registerContract362, deployment/wallet proving path already qualified separately; installed ContractFunctionInteraction.simulate returns the ABI-decoded value as result. Portal runtime immutable-bytecode qualification is not claimed here beyond deploying manifest-bound creation bytes and checking its getters/nonempty code.
