# T02 preparation: actual-proof journey gaps

Read-only preparation during U01 run036. No source changes, tests, network checks or graph status changes. Existing passes below are historical and source-bound; none automatically qualifies changed final artifacts.

## Acceptance map

| Gate | Existing actual evidence | Remaining work |
|---|---|---|
| A01 full journey | C01 application-c2a64fc6-366e-4460-aad3-dc8424ddf3af.json completes no-post deposit/claim/exit/refund in264255ms. W01 application-0543b83b-7ca8-406c-890d-074c5ff17299.json adds direct private fees,290068ms. C02 application-39636ef8-238a-423a-a490-8a07c8ba5ce7.json proves two posts with authentic mature-child screening,335408ms. | One integrated flagged-post→screen→eligible withdrawal→real Outbox refund scenario is absent. Unflagged posted exit and redeposit receipt lifecycle also need integrated coverage. |
| A02 exact transition state | Existing helpers check exact active deposit/replacement notes, nullifiers, public post text/length/time and private fee debit. c01-withdraw-l1 checks actual Outbox witness, active receipt, liability and portal balance reduction, depositor gas-adjusted refund and event. | Carry those assertions through flagged screening: flag/reason, screened sequence, preserved penalty/time debt, rejected early withdrawal, later successful withdrawal, then fresh nonce/deposit-chain on redeposit. Do not infer these from disjoint passes. |
| A03 hostile origin/replay | T01 has171 TXE/constraint tests and37 Solidity tests; these are not171 actual cryptographic proofs. C02 wrong-chain and mutated randomness/nonce cases reject during constrained membership work, followed by genuine valid inclusion. W01 application-8df5c9f5-4b7d-4a4c-9a90-4d726f9748e5.json proves stale post conflict/replacement; application-74babdac-4283-478d-97ae-5dd5fa9c0909.json covers genuine same-note dummy/withdraw attribution and refund. Real L1 refund helper rejects replay. | Add wrong portal/message/receipt identity and wrong-origin note attempts in the real-proof environment where currently only TXE/client fixtures cover them. State which failures occur before proof, during witness/constraint execution, or in node verification. Assert no inclusion/no altered balances/notes and no client success. Include old receipt replay after redeposit. |
| A04 public testnet | These reports explicitly use disposable local chain31337, real application proofs and controlled local settlement. | Separate external compatibility gate: establish a suitable pinned stack/environment and disposable funding. If unavailable, record concrete environment blocker/unblock condition. Do not claim local evidence qualifies a public network or silently waive A04. |

## Minimal implementation sequence

1. Reuse existing C01 setup, deposit/claim, normal inclusion, official local settlement and refund helpers. Add a bounded journey profile that continues after a real post instead of returning at the existing screening/private-fee-post branch. Reuse C02's actual maturity anchor and note identity checks, but perform an authorized moderator flag and standalone screen before the exit. Current C02 explicitly excludes flagged cases; its successful second post screens an unflagged child.
2. Check pre-screen/early-time withdrawal rejects without state changes. Advance the disposable chain using existing supported controls, then prove withdrawal normally. Feed its actual message to existing settlement and L1 refund helpers. Verify private fee balances separately from ETH collateral liability throughout.
3. Follow successful refund with another real deposit from the same depositor; assert nonce and receipt-chain separation, reject replay of old claim/refund data, and complete the fresh no-post lifecycle. If this and the flagged scenario cannot both fit one540s budget, use separate independently capped profiles with fresh fixtures and explicit composition scope. Never extend the cap or remove the node from aggregate2GiB accounting to obtain a pass.
4. Add the unflagged-post exit variant by reusing the same profile with flagging absent; existing no-post genuine coverage can be reused only after exact source/artifact reconciliation, otherwise rerun that existing profile. Avoid another ten-author contention run for this package unless its relevant inputs changed.
5. Bind emitted artifacts and current source hashes to every run. Preserve sanitized receipts/configuration/timings, success and hostile-case observations, cleanup result and aggregate RSS. Have an independent agent review the integration after implementation; that remains internal review, not external audit.

## Compiler and qualification boundary

T01 acceptance-012 preserves all26 original compiler observations and the fresh007 inventory (57diagnostics/18sites), with independent warning dispositions still open. T02 must reconcile against its final emitted application/protocol artifacts and carry the adverse proof/privacy obligations forward explicitly; do not substitute the finite models or source reasoning for proof/privacy evidence. T01 finite models are bounded executable models, not cryptographic/code-equivalence proofs. W03 remaining-stage-coverage.md explicitly says live combined regenerated-screening race is not qualified by existing attribution tests.

Existing bounded entrypoints: `node scripts/test-c01-application.mjs --bridge`, `--screening`, `--private-fees`, `--private-fee-post`, `--proof-recovery`, `--note-attribution`. Use pinned Node24 and the existing supervisor. These are command inventory, not newly executed tests. No epoch prover, mocked Inbox/Outbox, disabled prover or browser GUI-success-only substitution qualifies T02.
