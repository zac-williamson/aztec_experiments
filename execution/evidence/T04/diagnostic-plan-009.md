# One bounded diagnostic of the immediate deposit failure

007 passed handoff but failed before any proof. Independent driver and application source inspection found no concrete cause; report omitted deposit status. No application repair is claimed.

The next run changes observation only: existing error formatter wrapper retains its original receiver/arguments/result and exports allowlisted exception class/code plus self-source line/column (no message). Journey mode skips debugger breakpoints. Fixed driver substages and exact milestone/error booleans distinguish driver failure from app deposit/secret-storage/claim progress. The native coordinator atomically persists the existing sanitized RPC observer snapshot before cleanup. Parent allows at most1500ms within the same global budget for this snapshot before stopping its owned tree.

Cheap tests cover fixed diagnostics against secret-bearing input, shared handoff, cancellation, RPC boundary and observer sanitization (diagnostic-checks-009.log). No endpoint, proof or custody behavior changed. Attempt010 uses unchanged540s/2GiB and is for diagnosing007; it does not represent a speculative fix or broader performance qualification. Retain its outcome, then inspect exact failure location before further work.
