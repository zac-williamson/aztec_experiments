# Cold fee handoff amount mismatch

Pinned viem formatEther(10**18) returns "1". The original handoff required a
decimal point, so a known future failure was found during090 setup. Supervisor
was deliberately sent SIGTERM at36776ms beforebrowser launch; allcleanup passed.
This was not a observed application failure. Failure evidence is preserved.

Accept positive whole or fractional decimal strings (up to18fraction digits),
reject signs/exponents/leading zeros/zero, and match publicationdriver validation.
The regression uses actualpinned formatEther output. Focusedchecks and fullharness
091 pass; independent structural review approved. No alternative amount, route,
retry loop, deadline extension or application workaround was introduced.
