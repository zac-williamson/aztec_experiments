# Genuine GUI lifecycle harness implementation

Added fixed `--browser-journey` mode using existing supervised disposable protocol fixture, native warm private-fee funding, shared actual HTTPS browser bootstrap and real DOM deposit/claim/post/screen/withdraw/refund actions. Existing `--browser-post` follows its original branch. Whole-test540s/2GiB sampled owned-tree limits and cleanup remain unchanged; source inventory includes new helpers and outer wrapper.

Parent requires exact actual captured browser submissions under normal node proof validation. Public stage signals contain only bounded hashes; native parent independently verifies canonical claim/post/screen/exit receipts/effects, real L1 Deposited event/transaction/amount/canonical block and actual exit leaf before settlement. Client mining unwinds before official local Outbox settlement. Browser performs the L1 refund; parent verifies gas-adjusted payout, escrow liability/balance, exact Withdrawn and Outbox-consumed event/leaf state.

Browser closes before fresh read-only PXE opens. Final verifier checks historical physical deposit-note chain and consumed nullifiers, exact packed sequences/cooldowns, real and dummy post notes, public content and inclusion time, screening against the public moderation deadline, final absence, private debit/public protocol fees and author public balance zero. Each captured transaction has semantic public-footprint classification and RPC summary. This is warm private-fee setup, not browser fee-funding or external wallet-extension qualification.

Review corrections: remaining-time poll uses raw deadline and lifecycle AbortSignal (including no shadowing); actual abort test covers clamped legacy callback. Public moderation wait uses inclusion timestamp rather than private anchor. Deposited event binds actual receipt/log block hash and transaction; consumed event matches message hash, leaf ID, epoch and checkpoint count. Installed SDK source confirms getBlock includes header, historical spent-note discovery and Outbox API names.

Lightweight verification: six tests pass (five journey adversarial/helper tests plus existing browser RPC boundary test), ~0.71s. Syntax and whitespace checks pass. No genuine browser/proof/build run performed by this agent. Parent owns one source-frozen attempt and independent outcome review; no application acceptance claimed by helper tests.
