# P04 Linux retry2 — root installation passed; source-copy permissions failed

This is **not a Linux build pass**. The fresh locked root npm installation completed successfully in422.63seconds, with timing diagnostics preserved. The longer infrastructure allowance allowed completion beyond the previous300second budget; this outcome is not a package failure. The subsequent portal installation failed in0.23seconds with exit243 and `EACCES` creating `/work/billboard/portal/node_modules`.

A separate tiny disposable probe reproduced the exact metadata cause: effective container UID0/GID0 could write `/work` (owned0:0), while directories copied by Docker retained host UID501/GID20 and mode0755. With all capabilities dropped, UID0 could not write those directories. This is a source-staging failure, independent of package resolution. See `clean-linux-copy-permission-probe.json`; the probe was removed successfully.

The actual retry ran on the pinned Node24.15.0 Linux/aarch64 image, with3GiB/two CPUs, `GOMAXPROCS=2`, no mounts, no exposed ports and all capabilities dropped. Fresh-source/cache checks and checksum-pinned Foundry1.4.1 passed. All147 source inputs matched inside the container before execution and after the failure; `clean-linux-retry2-result.json` also records the final comparison to root. No host home, secrets, legacy RPC credential, dependency cache or Docker socket was copied or mounted.

Compiler bootstrap, contract/SDK/CRS/app builds, independent derived-G1 generation, all build/interface/Noir/portal/CLI tests and the34-output comparison were **not run**. The intended native reference is `derived-crs-root-output-hashes.json`, SHA-256 `f0f24b0338535d0e20c7b290155fad29f6bf7ccfa0222380b85350039e3cfb30`. No old output comparison is substituted for the missing run.

Container `billboard-p04-retry2-999bc8ad24` exited1 with `OOMKilled=false`. All stage logs and npm timing JSON were extracted before removal; extraction and owned-container removal both succeeded with exit0. No container from this retry remains active. Original failure reports are preserved.

Evidence: `clean-linux-retry2-inputs.json`, `clean-linux-retry2-final-preparation.json`, `clean-linux-retry2-container-inspect.json`, `clean-linux-retry2-container-evidence/`, `clean-linux-retry2-run.json`, `clean-linux-retry2-result.json`, and two install-progress snapshots. Complete helper source is preserved in final-preparation. Before another costly installation, normalize archive ownership to the container user without changing source bytes, test write access in root and nested source/build paths, retain capability restrictions, and obtain root review of the concrete staging fix. No further full retry has started.
