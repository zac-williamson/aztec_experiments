# P04 acceptance — supported V5 foundation and interface freeze

All five P04 criteria now have evidence for this package's scope: a tested
interface design and supported, reproducible Aztec5.2 development foundation.
This is not production application acceptance. The contract and service defects
assigned to subsequent graph packages remain to be implemented and verified.

The final application fingerprint is
`750cb4c1df4c99b7140e252bf987b66ac39ced85ee48632d8c1f6e74e4b103cc`,
recorded again in `source-verified-2026-09-14.json`. Independent reconciliation
found no changes across268 inventory entries,147 Linux build inputs or the two
intentional internal symlinks. The four frozen interface specifications/fixtures
are separately content-bound because execution documentation is excluded from
that application fingerprint.

## Observed checks

- The independent clean Linux build regenerated every artifact from its pinned
  inputs and matched all34 native outputs byte for byte. Its171 build guards and
  69 interface/storage/baseline checks passed. Full compressed and derived CRS
  bytes, compiler source origins, Solidity metadata, SDK and workers are included.
- After the user rebooted, both fresh Chrome consumers passed actual initialization
  of the75,497,472-byte/1,179,648-point derived setup, with Buffer, Poseidon, worker,
  SQLite and cross-origin isolation checks. Existing30s navigation/120s evaluation
  limits were unchanged. Total supervised duration was5.838s; browsers and server
  closed normally. The independent browser reviewer verified source/evidence hashes.
- Focused native checks passed22 Noir layout/commitment cases,7 Solidity transcript
  cases,9 portal regression cases,5 offline SDK/CLI lanes and62 Noir/TXE cases.
  The full62-case run completed in263.476s. These are real compiler/TXE/contract
  test executions, not genuine application or rollup proof acceptance.
- All34 generated outputs matched before and after the native tests. Source
  inventories remained unchanged and owned test processes were reaped.
- The earlier155 moderation cases, fresh-store browser persistence, canonical
  HandshakeRegistry recomputation and official source-lock evidence remain bound
  to unchanged relevant inputs. The graph's29 tests also passed on September14.

## Failure disposition and exact limits

Prior Docker, browser and Linux fixture failures remain in their original logs.
The Linux fixture timeout is still an incomplete Linux fixture attempt. The new
native22-case pass supplies focused current behavior evidence; it does not rewrite
that attempt as successful. The independent clean Linux build and output comparison
are separately successful qualifications.

The first resumed native attempt could not acquire Nargo's dependency-cache lock
or bind TXE's loopback port inside the sandbox. Those two stages were repeated with
scoped execution permission and passed without source, assertion or test-limit
changes. Already-passing stages were retained. No old process ID was reused after
the reported reboot, and no unrelated process was terminated.

P04-A01–A04 establish exact design/serialization and consumer handoff agreement.
They do not establish runtime note authentication, concurrent posting, complete
history, penalty economics, private fees or authenticated cross-chain withdrawal.
Those remain C01–C06 and W/F/M/D/T work. Fresh deployment has one intended ABI/note
format and no legacy migration requirement.

The26 compiler constraint diagnostics still require actual proof/security review.
Exact-lock dependency advisories remain A02 work. X03 target clearance, independent
external review, real-proof journeys, operator acceptance and14 days of representative
soak remain mandatory. The application is not production-ready.
