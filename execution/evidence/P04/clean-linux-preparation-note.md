The initial copy conservatively rejected two existing relative source symlinks in the censor app. A launch was mistakenly attempted after that copy failure; the disposable container exited 127 immediately because its script had not been copied. No build or tests ran in that attempt. The partial task-owned directory and failure log/result were preserved. The copy helper was corrected to preserve only existing app-source symlinks whose resolved targets remain inside apps/src, and records both link targets and content hashes. No application source was changed.
