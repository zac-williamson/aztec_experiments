# P04 closure evidence audit

Read-only delegated audit on 2026-09-12, begun while the supervised Linux retry3 was in progress and finalized against its extracted result. No application/graph edits, tests, builds, browser launches or proof runs were performed in this audit lane. The only file written in this lane is this audit. This maps the current evidence; it does not mark P04 complete.

## Closure assessment

**The independent Linux build and full 34-output comparison passed. Actual browser qualification of both derived-CRS consumers remains open, and the additional Linux Noir interface fixture needs a focused complete run to resolve its timeout.** Retry3 passed 171 build guards and 69 interface/storage/baseline cases, then its fixture reached the unchanged 120-second inner deadline after 10 of 22 unique cases reported success. No assertion failure was reported; the remaining cases are unverified in this attempt. Later Solidity/portal/CLI/Noir suites were not run. This is a successful reproducible build and an incomplete Linux test suite. Beyond resolving those verification limits, root needs final evidence reconciliation and the normal package acceptance record. I found no missing interface-design deliverable requiring another implementation lane or a blanket repeat of unaffected tests.

P04 is an interface-freeze and compatibility package. A01–A04 can be evidenced as tested design/serialization agreement, with their runtime obligations explicitly handed to C01–C06 and W/F/M/D packages. Their evidence must not be described as proof that the current application already enforces those properties. In particular, A02 does not yet demonstrate ten-author concurrent posting, and A04 does not demonstrate actual cross-chain burn/refund atomicity. The specification clearly states that distinction and names the downstream tests.

## Criterion-by-criterion mapping

| Criterion | Concrete evidence available | Honest closure scope and remaining action |
| --- | --- | --- |
| **A01: serialized IDs, hashes, authorization, note bindings, schema versions** | `execution/interface-spec.md` §§1–4, 6–7 defines exact wire types, Poseidon domains/input order, SHA transcripts, actor/owner/slot/contract/deposit-chain bindings, and V1 notes. `interface-design.md` records the source/API review. `note-layout-tests.log` records actual beta.25/Aztec 5.2 **22/22** pure layout/range/Noir-hash cases; `solidity-commitment-tests.log` records **7/7** independently constructed Solidity transcripts. JS encoder checks include corrected pre-portal config scope; `integrated-interface-tests.log` records the final **69/69** combined interface/storage/baseline run. | Design/codec criterion supported. Pure packing and hashing do not authenticate hinted notes or bridge messages. Poseidon identifier known-answer vectors and real note retrieval are explicitly assigned downstream, rather than represented as already exercised. Bind final specs and both shared JSON fixtures as artifacts. |
| **A02: no private reservation of global post counter; distinguish per-deposit history** | Contract specification §§4–5, 7 reserves independent random post IDs and assigns public order in live `_publish_post`; private proofs neither read nor supply a reserved global count. `depositChainId` and sequence separate each private history, with explicit selection/authentication requirements. Typed layout/selector cases verify the proposed physical field positions; shared schema cases keep `postId` distinct from public order and reject private linking fields in public records. | Interface invariant supported. Existing production contract behavior is still known bad and C03 owns actual same-anchor concurrency; C02/C04 own authenticated per-deposit query/history behavior beyond fixed pages. Do not relabel mocked positions or selector construction as those runtime results. No extra P04 implementation is needed. |
| **A03: wallet/feed/moderation/deployment fixtures and failure/retry agreement** | `execution/service-interface-spec.md`, `shared/protocol-schema.mjs`, `shared/protocol-commitments.mjs`, and `execution/interface-fixtures/{service-v1,commitments-v1}.json` are the single handoff. `service-interface-review.md` resolves cursor order, receipt lifecycle/execution separation, deadline width and deployment order. `config-scope-tests.log`/`config-scope-check.json` cover computing configuration before portal deployment. Final cursor correction is bound by `post-freeze-cursor-delta.json` and `integrated-interface-tests.log`; actual matched SDK cursor/receipt classes are exercised. | Shared wire/failure semantics supported. Real leases, durable journals, event-origin/reorg validation, fee reservations and modelVersion implementation remain downstream. The docs explicitly forbid automatic public-payer fallback and treating uncertain/reverted outcomes as success. Current consumer runtime migration is not claimed by these fixtures. |
| **A04: recovery/penalty rules preserve rights/refund and unique withdrawal** | Contract specification §§2–3, 8 requires one active receipt, monotone nonce, actual right burn before an authenticated unique exit, all-case expired debt, checked finite eligibility, rollback on bridge/ETH failure, and no timeout/admin refund with live rights. Service receipt/journal rules preserve uncertain submissions and require cross-chain consumption, not only an L2 receipt. `interface-design.md` and `service-interface-review.md` reconcile fresh-deployment recovery semantics. Distinct claim/exit transcript vectors bind the same receipt identity under separate domains. | Design invariant supported. Economic execution, reentrancy/rollback, no-post/debt withdrawal and real proof atomicity are C01/C05/C06/T acceptance. No new legacy migration path or discretionary refund is missing from P04; the user excluded it. Keep those limits in the criterion result. |
| **A05: supported matched V5 stack, selectors/canonical assumptions, reproducible build and affected baselines** | `compatibility-preparation.md` cites official 5.1/5.2 compatibility; `toolchain-migration.md`, `toolchain-versions.json`, `toolchain-noir-origins.json` and source-lock checks bind 5.2/beta.25 and six packages/413 files. `handshake-consistency.json` recomputes artifact class/address and compares SDK and locked Noir constants. Existing **62 Noir/TXE**, **155 moderation**, **69 interface/storage/baseline**, actual browser store persistence, offline CLI SDK cases, and note/Solidity fixtures are retained. Native/root historical builds matched 33 outputs. New derived build/runtime passed **171/171** guards and cold full-size derivation; `derived-crs-independent-review.md` closes the stream-cleanup finding. | **Open.** Retry3 now independently regenerated all outputs from absent caches and passed the complete **34-output** comparison, current source/recipe attestation, evidence extraction and owned-container cleanup. Its final result records **171/171** build guards and **69/69** interface/baseline cases passed, followed by an incomplete Noir fixture (10/22 unique cases reported passing before the unchanged 120-second timeout); later suites were not reached. Preserve that distinction and resolve the fixture with a focused complete run. Require actual browser success for both consumers on the exact derived representation with unchanged budgets, plus existing worker/SQLite checks. A failed navigation/initialization or pending run does not pass. No real application/rollup proof or X03 clearance follows from this package. |

All paths in the table are relative to `execution/evidence/P04/` unless a repository path is explicitly written.

## Source freshness checks performed

Read-only hashing found all **147** entries in `clean-linux-retry3-import-sideeffect-inputs.json` present and identical to the current workspace. `clean-linux-retry3-launch-attestation.json` records their agreement and the current 34-output reference. The `source-derived-crs-candidate.json` inventory also matches: its two absent paths are explicitly represented as `DELETED` (`apps/dist/thread_worker.js` and the old portal build-info file), not missing evidence. Its fingerprint is `750cb4c1df4c99b7140e252bf987b66ac39ced85ee48632d8c1f6e74e4b103cc`.

The storage-source map, offline CLI source hashes and HandshakeRegistry source/lock hashes still match the recorded inputs. The current SDK bundle remains the same content used by the successful storage browser and offline CLI checks. The derived change affects CRS build/client/manifest and generated HTML, not note layouts, Noir contracts, moderation logic, or the source-built SDK bundle. `derived-crs-output-delta.json` records 28 unchanged outputs, five changed outputs and one addition. This supports retaining unaffected earlier results with explicit source/delta reconciliation; no evidence found here requires another 62-case Noir or 155-case moderation run solely because derived CRS was added.

Current executable specification/fixture hashes:

| Input | SHA-256 |
| --- | --- |
| `execution/interface-spec.md` | `e3ed67c6cba40ba481fa40e691b0b79c8c3462fdc4c50ff03173d665e334be57` |
| `execution/service-interface-spec.md` | `d99518d53ca78e508ce041f054f7be9f065275ccbf8ee99857e5dca9583e2897` |
| `execution/interface-fixtures/commitments-v1.json` | `e988b16a0bc12fc2ab6423b429cd7485f1b5174fc4ed09915314b6e6ce1f9b92` |
| `execution/interface-fixtures/service-v1.json` | `227e07da682a7fac808d9d5e509eca5654b2870a77a453458d9a8a30407de77a` |

The final source fingerprint intentionally excludes `execution/`. Therefore it **does not by itself bind these specifications or JSON vectors**. Root has now preserved all four current files under `frozen-interfaces/` with `frozen-interface-inputs.json`. Read-only byte/hash checks confirmed all four copies are identical to their current source files and match the full digests above. Link this inventory and the actual copied artifacts in the final P04 record; the application fingerprint alone is insufficient. The Linux input inventory already explicitly includes the vector files.

The final Linux outcome is bound by `clean-linux-retry3-report.md`, `clean-linux-retry3-result.json`, `clean-linux-retry3-comparison.json` and `clean-linux-retry3-final-attestation.json`. Both output inventories hash to `f0f24b0338535d0e20c7b290155fad29f6bf7ccfa0222380b85350039e3cfb30`, with 34 matches and no differences or omitted artifact fields. All 147 source files and two internal links remained matched. The container exited 1 because the fixture timed out, was not OOM-killed, and was removed after successful evidence extraction. The fixture's post-test external dependency check was not reached; the source-file attestation does not replace it. These records provide the Linux build pass without representing unrun test stages as passed.

## Bookkeeping and remaining qualification

1. **Create the final `execution/evidence/P04.json` only after A05 passes.** No P04 acceptance record existed at audit time. Include all five criterion results, the source inventory, exact reviewed artifacts/logs and these limits. Perform the normal graph validation and source reconciliation at closure; this audit did not run them or alter state.
2. **Keep the corrected ledger current.** The latest `acceptance-status.md` now distinguishes retry2's portal-install EACCES failure, the reproduced ownership fix with 40 writable-directory controls, and retry3's successful build/34-output comparison plus incomplete additional fixture. It also records successful extraction/cleanup. This resolves the stale running-retry description found in the preliminary audit. Its browser gate remains open: the last 30-second navigation timeout occurred before evaluation. The post-cleanup observation of 9,527.62 MiB used swap and load 17.50 is resource context, not a proven timeout cause or grounds to waive a check. Add a later actual result when conditions permit; no same-condition retry or unrelated process termination is requested here.
3. **Use the final cursor fixture provenance.** `config-scope-check.json` includes the older service fixture digest `f88f92e077b8f13d6e85a615aa39f4a10b0667758715bf374fd4ec68fffcf4dc`, from before the cursor correction. Its config-encoder and commitment-vector evidence remains useful, but its service-vector entry is historical. The current `227e07da682a7fac808d9d5e509eca5654b2870a77a453458d9a8a30407de77a` digest and affected checks are already present in `post-freeze-cursor-delta.json`/`final-input-delta.json` and the integrated interface log. Preserve `config-scope-check.json` as the historical observation; explicitly link those later records and `frozen-interface-inputs.json` as the final current service-fixture binding. Do not rewrite its past digest as if that older run used the later fixture. No regeneration of correct vectors is required.
4. **Preserve source-specific review status.** `consumer-routing-review.md` and earlier build reviews include historical `apps/build.mjs`/CRS hashes. They establish routing observations for that snapshot, not final derived behavior. The later derived review binds the changed build/runtime/comparator/browser-test files. The final acceptance narrative should connect those reviews through the recorded delta, not call every historical table current.
5. **Keep exact-lock residual work visible.** The 26 compiler diagnostics are explicitly unresolved security qualifications in `compiler-diagnostic-review.md`; compilation does not classify them as false positives. Dependency advisory remediation/disposition is A02. Real proof/privacy cases and external review remain their assigned gates. The current X03 recheck found no official clearance lifting the V5 pause. None needs to be silently waived to freeze interfaces; none is satisfied by P04.

The Linux build qualification is complete for these inputs. The browser gate and the focused incomplete fixture still require actual qualifying results; retain A05 as incomplete until resolved. Successful earlier native results remain valid for their recorded sources, but cannot turn retry3's timed-out or unrun stages into Linux passes. Source changes require checks affected by the actual change, not automatic repetition of every unaffected suite. After the remaining checks pass, final evidence assembly, review reconciliation and normal graph closure remain; no new interface implementation was identified. This audit supplies no browser, Linux, compilation, proof, deployment or external-assurance pass of its own.
