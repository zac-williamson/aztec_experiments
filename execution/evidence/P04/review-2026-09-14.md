# Root P04 integration review

Root reviewed the final evidence with independent AI lanes build_review,
build_verification and artifact_regressions. This is internal AI review, not the
external Aztec/Noir/Solidity review required for release.

The frozen specification and shared fixtures cover the defined transcript domains,
note layout and authorization requirements, publication/history identity, service
failure states and refund/right invariants. The known implementation defects remain
assigned to downstream packages; accepting an interface must not mark those fixes
as implemented. The final cursor fixture supersedes only the historical cursor
entry in config-scope-check, preserving the earlier observation.

Source reconciliation, actual fresh browser records, clean Linux34-output comparison
and resumed native test results support the current foundation. The derived setup
retains every point and binds complete compressed/G2/WASM/output content; the runtime
verifies complete derived bytes and uses format-specific API response checks. The
independent cleanup finding was fixed and included in171 passing build guards.

Root checked the raw native stage exits, compiler fixture counts, browser supervision
and source/output comparisons. Sandbox permission failures and the earlier Linux
timeout are explicitly retained. Runtime observations are not presented as product
proof benchmarks or genuine rollup settlement. Current source has not changed since
the candidate was frozen; no redundant clean build or broad unaffected suite rerun
is required solely because the host restarted.

The review retains all external and later engineering gates, including dependency
advisory remediation, compiler diagnostic qualification and the V5 deployment pause.
The acceptance record must hash the four frozen interface files as well as source,
test logs and reviews. A local commit records this coherent foundation; it neither
publishes the project nor authorizes production deployment.
