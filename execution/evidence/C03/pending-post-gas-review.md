# Pending-post gas/config review

Read-only review of application-5f2c706f-9204-437b-bd93-ed3f1a728c19 and installed5.2 code. No runtime or source changes.

Observed: all10 genuine post proofs were prepared at the same anchor, normal node validation returned valid, all10 submissions succeeded, and all10 remained pending until the helper120s inclusion wait failed. Recorded declared gas is6540000L2/271200DA, with teardown817500L2/135600DA. There is no included reverted receipt. Logs show repeated “No blocks were built”, some slot mismatch, and no recorded declared-gas skip warning. Cleanup succeeded. This does not qualify contention or establish its cause.

The proposed raw24-block gas mismatch is not demonstrated. The recorded sequencer config maxBlocksPerCheckpoint24 is a ceiling, not the effective gas divisor. CheckpointProposalJob supplies `this.timetable.getMaxBlocksPerCheckpoint()` to block-builder options. ProposerTimetable computes and clamps actual subslots from slot/block/operational budgets. Under the shown12s slot,2s block,1s Ethereum and pinned fast defaults, the formula is floor((12−1−2−2×0.5−0.5)/2)=3, then min(3,24)=3. Actual live multiplier/mana budget were not captured, so do not infer a numeric L2 cap from config24. The first-block DA fair share from786432 at default1.5 multiplier and3 blocks is393216, above the declared271200; blob encoding has its additional bound.

PublicProcessor.process checks `totalBlockGas.add(tx.data.constants.txContext.gasSettings.gasLimits).gtAny(maxBlockGas)` before execution and emits a warning when skipping. That check occurs before `processTx` chooses public-call versus private-only handling, so it also applies to the successful private claims. Public/claim divergence cannot be explained by that check alone without differing live limits or starting budgets. CheckpointBuilder.capLimitsByCheckpointBudgets still enforces remaining checkpoint budget and per-block allocation, so actual declared/remaining figures remain worth observing.

The “No blocks were built” outcome also occurs before public execution: `buildBlocksForCheckpoint` can find no viable timed subslot; `canStartBuildingBlock` waits for `p2pClient.hasEligiblePendingTxs(minTxs)` and abandons at the start deadline. A pending receipt does not establish selection eligibility. The next discriminating diagnostics are effective timetable count/multipliers, eligible count versus pending count, selected subslot/deadline and whether PublicProcessor.process is actually entered. Capture only bounded numeric/public status data. No reason to launch another full attempt solely on a presumed gas fix.

Primary local source: `@aztec/sequencer-client/dest/sequencer/checkpoint_proposal_job.js` options around1366 and eligibility around1490; `@aztec/stdlib/dest/timetable/proposer_timetable.js` constructor and computeMaxBlocksPerCheckpoint; `@aztec/stdlib/dest/timetable/budgets.js` fast profile; `@aztec/validator-client/dest/checkpoint_builder.js` capLimitsByCheckpointBudgets; `@aztec/simulator/dest/public/public_processor/public_processor.js` process gate around544 and public/private dispatch around773. These are installed pinned source observations, not a protocol audit.

## One-author control and bounded batch geometry

Root's one-author diagnostic application-725e6a89 passed in202.9s with the same declared6540000L2/271200DA, exact successful public post and effective timetable count3. It is explicitly nonqualifying for ten-author contention. This rules out an unconditional failure of the new post operation at those limits in that local run, but does not establish the cause of the ten-author pending backlog.

Pinned node isValidTx uses rpcProofVerifier by default; only explicit isSimulation bypasses that verifier. A later admission result may benefit from the actual verifier's cache but is not a fake proof check. P2PClient.getTxStatus maps protected to pending, whereas eligible iteration uses the pending index filtered by minTxPoolAgeMs. Protected transactions can therefore produce pending receipts without being eligible. TxPool.prepareForSlot clears expired protection and revalidates/restores metadata; an observation of pending receipts alone cannot identify this state. No evidence here establishes ten-proof verification exceeding an attestation budget.

Proposed maxTxsPerBlock1 for the post stage is an acceptable local experiment, provided it is restored afterward: C03-A01 demands ten distinct authors preparing genuine proofs from the same anchor, not simultaneous public execution or throughput. All ten proofs must remain prepared before any submission; exact inclusion/state/order checks remain unchanged. Cap1 deliberately removes public batch capacity from this application invariant test. It must be reported as test geometry/experimental mitigation, not a diagnosed fix or production capacity claim. No proof/concurrency/time limits should be silently relaxed. No runtime or source edits by this review lane.

Root's implemented run configuration sets maxTxsPerBlock1 at disposable node construction for both claims and posts, instead of a live post-stage change. Parent cleanup removes the entire node, so live configuration restoration is unnecessary. This preserves the acceptance rationale above: same-anchor ten-proof preparation and all ordinary successful inclusion checks remain mandatory. The active run keeps native one-thread proving, genuine node verification and540s supervision. Outcome is still pending; no edits to frozen inputs.
