# C02 genuine-proof lane preparation

Unimplemented design only; C01 retry source remains untouched. Reuse the qualified
actual Ready/portal activation and real deposit/claim helpers. A separate explicit
screening profile should stop after client screening checks; do not relabel it a
full refund journey. Extend graph C02 edit boundaries to the required scripts when
activation is authorized by completed C01.

A discriminating low-cost genuine control can use one Aztec account and two fresh
Ethereum depositors on the same activated board. Fund the second disposable L1
account by an ordinary transfer from the harness's freshly generated account;
never use a saved user identity. Existing depositAndClaimC01 scopes note checks by
creation transaction and private chain, so it can claim both real deposits for the
same preparation.account without changing its exact-note/replay assertions. The
second L1 client must preserve the same local RPC and actual chain identity.

Under the existing stopped-agent/continuous-L1-mining wrapper, claim both deposits,
wait genuine eligibility anchors, and prove/include an initial real post under
each chain. Query each actual HintedNote. Attempt the next post on chain B with
chain A's real included hint and require the specific C02 wrong chain constrained
execution error. Do not mutate its metadata or count a generic oracle error as the
expected rejection. Check original transaction inclusion and exact active deposit
notes before/after; then prove/include a legitimate B screening transition with
B's own hint and compare exact screened sequence/link and one replacement note.
Preserve source/artifact hashes, proof hashes, ordinary node validation and
successful checkpoint receipts; keep all private history/identities in memory.

This complements TXE foreign-owner/contract/slot controls rather than claiming
all hostile origins have genuine network proofs. No synthetic Inbox message,
verifier, Outbox root or accepted fake proof is permitted. The successful screening
transaction's client proof and normal node admission/inclusion are the intended
real-proof evidence; separate C01 evidence already covers genuine server settlement
and refunds. Later integrated release qualification still reruns the full journey
on the final application candidate.

No adapter is implemented or tested by this note. Before running, verify the
current artifact's public post_count conflict behavior, exact fee options, private
hint conversion, and inclusion-time assumptions against C02/C03/C05 ownership.
Keep no more than one native proof process active. The second anonymous owner or
second board is unnecessary for the concrete cross-deposit attack above and would
increase setup work without improving this particular control.
