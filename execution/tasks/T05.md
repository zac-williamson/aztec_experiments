# T05 — Reverify all acceptance criteria on the final candidate

Generated from execution/graph.json. Edit the graph, then run graph.py render.

- Phase: release
- Type: internal
- Prerequisites: R02
- Requirements: REQ01, REQ02, REQ03, REQ04, REQ05, REQ06, REQ07, REQ08, REQ09, REQ10, REQ11
- Baseline findings: production component / release requirement
- Evidence: execution/evidence/T05.json
- Evidence binding: current

## Purpose and context

Read execution/requirements.md, decisions.md and the baseline assessment, then inspect prerequisite outputs and the relevant source. Requirements and acceptance below define completion; this plan does not assert any implementation already exists.

## Source and edit boundaries

- `billboard/test/`
- `apps/`
- `censor-daemon/`
- `execution/`
- `.github/`

These paths identify the working area; some new service/test/output paths will be created. Read related dependencies as needed. Record any necessary expansion before editing unrelated components. Use actual pinned APIs and discovered build commands; do not assume a proposed test or helper already exists.

## Work

1. Rebuild and run the full required suite on the final candidate, including proof-enabled journey, privacy, browsers, workload and recovery.
2. Create release/acceptance-matrix.json linking every acceptance criterion of every completed historical internal package to fresh check artifacts and hashes.
3. Create release/manifest.json with exact source fingerprint, toolchain/configuration and all distributable artifact paths/hashes; verify each file.
4. Reconcile P04 compiler-diagnostic-review.md against the final emitted application/protocol artifacts. Preserve all 26 original locations and require the relevant adverse proof/privacy cases and independent disposition; do not suppress the checks or treat source reasoning as proof.
5. Produce release/dependency-inventory.json and release/dependency-dispositions.json bound to the current candidate, and reference them from the final manifest. Refresh npm and runtime/image advisories, inspect embedded tooling, test D01 residual controls and reconcile every A02 residual entry before passing T05-A05.

## Acceptance

- **T05-A01** — Every completed historical internal task criterion is reaffirmed against the current source in the machine-checked acceptance matrix.
- **T05-A02** — All required real-proof, privacy, contract, browser, workload and recovery tests pass on that same candidate.
- **T05-A03** — The release manifest covers actual distributables and all hashes verify; no developer-only or stale artifact is substituted.
- **T05-A04** — No secret material enters the release package or its evidence.
- **T05-A05** — The final release dependency inventory binds the exact lock, runtime, image and embedded bundles to fresh advisory results, actual packaged entrypoints, tested residual controls and independent dispositions; all A02 residual findings have current treatment and no unresolved release-blocking control remains.

## Verification and handoff

Record exact commands, environment, sanitized output, source inventory, expected/observed behavior and limitations. Use execution/evidence/README.md. Tests must exercise the behavior and fail for the relevant bad case where practical; mocked evidence cannot satisfy a real-proof criterion. Perform a separate diff/assumption review after implementation. Keep external review requirements distinct from self-review.

If blocked, record reason, evidence, unblock condition and next action in graph.json; continue another ready package. Before marking done, verify all criteria and prerequisite states, save linked evidence, run graph.py validate, and update status.md. No unchecked criterion may be silently waived.
